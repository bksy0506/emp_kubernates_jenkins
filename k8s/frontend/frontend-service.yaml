apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: my-app
spec:
  selector:
    app: frontend
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30001   # 사용자가 직접 지정 (30000~32767 사이)
      protocol: TCP
  type: NodePort        # ClusterIP에서 NodePort로 변경